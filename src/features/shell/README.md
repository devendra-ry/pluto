# Shell Feature

This feature owns application shell composition around feature content.

Current modules:

- `components/chat-layout.tsx` for responsive app layout and error boundaries.

Guideline:

- Keep top-level layout orchestration here, while domain widgets stay in their own features.